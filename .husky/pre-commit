if git secrets --list >/dev/null 2>&1; then
	git secrets --scan --cached
else
	echo "git-secrets is not installed. Install it, then run: git secrets --install && git secrets --register-aws"
	exit 1
fi

npx lint-staged
cd client && npm run typecheck
